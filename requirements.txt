# Install necessary packages (GPU version with CUDA 12.8)
# PyTorch (BGE/transformers 임베더 호환, CUDA 12.8)
torch==2.7.1+cu128
torchvision==0.22.1+cu128
torchaudio==2.7.1+cu128
# 설치 명령: pip install torch==2.7.1+cu128 torchvision==0.22.1+cu128 torchaudio==2.7.1+cu128 --extra-index-url https://download.pytorch.org/whl/cu128

# 공통 패키지
pandas>=1.3.0
numpy>=1.21.0,<2.0.0   # numpy 2.x는 호환성 문제로 금지
openpyxl>=3.0.0
regex>=2022.0.0
tqdm>=4.64.0
FlagEmbedding
jieba
openai>=1.0.0
transformers
sentence-transformers

# pa 전용
scikit-learn>=1.0.0
spacy>=3.4.0

# sa 전용
mecab-ko>=1.0.0
mecab-ko-dic>=1.0.0
mecab-python3>=1.0.0
